name: Deploy SIMOBI to VPS

on:
  push:
    branches:
      - main

concurrency:
  group: simobi-production
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Setup SSH
        shell: bash
        run: |
          set -e
          mkdir -p ~/.ssh

          if [ -z "${{ secrets.VPS_SSH_KEY_B64 }}" ]; then
            echo "VPS_SSH_KEY_B64 is empty or not found"
            exit 1
          fi

          printf '%s' "${{ secrets.VPS_SSH_KEY_B64 }}" | base64 -d > ~/.ssh/simobi_github_actions
          chmod 600 ~/.ssh/simobi_github_actions

          ssh-keyscan -H "${{ secrets.VPS_HOST }}" >> ~/.ssh/known_hosts

      - name: Test SSH connection
        shell: bash
        run: |
          ssh -i ~/.ssh/simobi_github_actions -o IdentitiesOnly=yes root@${{ secrets.VPS_HOST }} "echo SSH_OK"

      - name: Deploy to VPS
        shell: bash
        run: |
          ssh -i ~/.ssh/simobi_github_actions -o IdentitiesOnly=yes root@${{ secrets.VPS_HOST }} "bash /var/www/simobi/deploy.sh"
